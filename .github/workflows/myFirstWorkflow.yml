name: my-first-workflow
on:
  push:
  workflow_dispatch:
jobs:
  job1:
    name: JOB1
    runs-on: ubuntu-latest
    steps:
      - name: step-1
        run: |
          pwd
          ls -ltr
          hostname
      - name: step2 -checkout
        uses: actions/checkout@5.0.0
      - name: step-2
        run: |
          pwd
          ls -a
          ls -ltr
          hostname
          
