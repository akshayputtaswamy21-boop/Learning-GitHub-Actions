name: my-first-workflow
on:
  push:
  workflow_dispatch:
jobs:
  job1:
    name: JOB1
    runs-on: ubuntu-latest
    steps:
      - name: step-1
        run: |
          pwd
          ls -ltr
          hostname
      - name: step2 -checkout
        uses: actions/checkout@v7.0.1
      - name: step-3
        run: |
          pwd
          ls -a
          ls -ltr
          hostname
          
